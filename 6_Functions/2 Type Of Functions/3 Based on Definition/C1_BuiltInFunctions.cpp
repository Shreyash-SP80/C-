#include<iostream>


// = Built-in (Library) Functions
//   - Predefined in C++ (#include required).
//   - Example: sqrt(), strlen(), pow().

// Example:
#include <cmath>   // To use all the built in math functions 
using namespace std;

int main() {

    cout << sqrt(25) << endl;   // 5  sqrt() Built in function of math Library
    return 0;
}